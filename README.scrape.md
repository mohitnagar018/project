# Scrape headlines

Quick script to scrape headings from https://example.com using Playwright.

Usage:

1. Install Playwright if not installed:

```bash
npm install playwright
```

2. Run the script:

```bash
node scripts/scrape_headlines.js
```

The script prints JSON with the page URL and an array of found headlines.
